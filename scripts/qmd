#!/bin/bash
# QMD wrapper — Linux/Mac
# Auto-generated by super-intelligence installer

export HOME="\{{USER_HOME}}"
export XDG_CACHE_HOME="\{{USER_HOME}}/.cache"
export XDG_CONFIG_HOME="\{{USER_HOME}}/.config"
export INDEX_PATH="\{{USER_HOME}}/.cache/qmd/index.sqlite"

QMD_BIN="\/@tobilu/qmd/dist/cli/qmd.js"
if [ ! -f "\" ]; then
    echo "qmd not found. Install with: npm install -g @tobilu/qmd" >&2
    exit 1
fi

exec node "\" "\$@"
